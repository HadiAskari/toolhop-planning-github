# toolhop-planning-github

## Can create either Conda or Pip enviroment

## Add your API key to the run.sh file and change the model to the best OpenAI Model. Also the model is hardcoded (or set as defaults in the code so you can change it there as well.)
